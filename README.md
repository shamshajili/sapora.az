# Sapora.az — React + Tailwind starter

Commands:

```bash
npm install
npm run dev
```

Files of interest:
- `package.json` — scripts and dependencies
- `src/App.jsx` — example routes, Framer Motion, React Icons, Parallax
- `tailwind.config.cjs`, `postcss.config.cjs`, `src/index.css`
